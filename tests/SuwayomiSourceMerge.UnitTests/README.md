# Unit Test Project

- Run unit tests: `dotnet test`
- In Copilot agent workflows, use `#runTests` (CLI equivalent: `dotnet test`).
- Run mutation tests (requires Stryker.NET tool): `dotnet stryker --config-file stryker-config.json`
